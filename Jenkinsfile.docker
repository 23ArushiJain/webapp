node ('docker-client') {
    
      stage ('Code from SCM') {
        git credentialsId: '942fc9c0-0014-4e97-a417-803ff28c0c75', url: 'https://github.com/23ArushiJain/webapp.git'
      }
      stage ('Build') {
        sh '''mvn clean install'''
      }
      stage ('deploy to docker container')
    {
        sh '''docker build -t app:1 .
        docker run -d -p 8000:8080 --name web_app1 app:1'''
    }
}
